---
type Props = {
  name: `${string}_${number}_${number}.${string}`;
};

const { name } = Astro.props;
const src = `${Astro.url.pathname}${name}`;
const [_, width, height] = name.match(/^.+_([0-9]+)_([0-9]+)\.(.+)$/)!;
---

<img src={src} width={width} height={height} decoding="async" />
